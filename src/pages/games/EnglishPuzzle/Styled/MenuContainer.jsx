import styled from 'styled-components';
import { PLAYFIELD_WIDTH } from '../../../../containers/EnglishPuzzle/Game/constants';

const MenuContainer = styled.div`
  width: ${PLAYFIELD_WIDTH}px;
`;

export default MenuContainer;
